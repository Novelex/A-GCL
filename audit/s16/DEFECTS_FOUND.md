# S16 DEFECTS LEDGER — read-only baseline, Correction Gate 1
Created 2026-08-25. Working tree was CLEAN (0 uncommitted changes) and this file did
not previously exist, so creating it overwrote nothing. NOTHING was edited, committed,
deleted, moved, cancelled or submitted while producing this.
Baseline: HEAD `2ef2ce3`, branch `s16-verify`.

| ID | file / function | symptom | severity | affected artifacts | scope | required test | disposition |
|---|---|---|---|---|---|---|---|
| D1 | `s16_train.py:19` `GROUPS` | `GROUPS` lacked `EDGEMLP`; `GROUPS[arch]` raised `KeyError` for A7 | **BLOCKER** | both A7 E2E rows FAILED; all 24 A7 C6 units would have failed | INSIDE | A7 end-to-end + exact partition of trainable params | **FIXED (Gate 2)** — `EDGEMLP: inp=net.0., enc=net.3., head=head.`; `assert_groups_cover()` added; 14/14 tests pass, exit 0 |
| D2 | `_e2e_run.py` / `s16_worker.run` | E2E and production shared one artifact tree | **BLOCKER** | 122 legacy artifacts (454.6 MB) | INSIDE | E2E creates no production file | **FIXED (Gate 3)** — `s16_prov.root(ns)` gives disjoint `runs/prod/` and `runs/e2e/`; worker takes `ns`; poison/results/tallies are per-namespace; tests A1-A3, B1-B3 pass |
| D3 | `s16_worker.py` resume | filename existence alone satisfied resume | **BLOCKER** | 16 fold files would have been silently accepted | INSIDE | 10+ mismatch classes must each rerun or fail loudly | **FIXED (Gate 3)** — 26-field provenance manifest per fold; `validate_reuse()` checks every field plus feature/checkpoint hashes; C1-C17 pass, each rejection naming the field under test |
| D4 | `s16_collect.py` `guard()` | no completeness check | HIGH | any future results CSV | INSIDE | refuse when units < 159 or folds < 1431 | **FIXED (Gate 3)** — guard now checks tally count, `UNIT.done` count, attempted-fold count, cross-namespace tallies and the `accounting_ok` identity |
| D5 | `s16_worker` / `s16_feat.alff_scaled` | ALFF z-scoring fitted on full `tr` (763) while the encoder trains on `tr_enc` (610); `build_X` was also called BEFORE `honest_split`, so the split did not yet exist | HIGH | every ALFF-carrying arm (A1, A4, A6, ABL) | INSIDE | perturbing only `tr_prb` must change neither the fitted parameters nor transformed `tr_enc` | **FIXED (Gate 5)** — split now precedes `build_X`, which takes `tr_enc`; A1-A6 pass (max\|dmu\|=0.0, tr_enc and te bitwise unchanged, tr_prb demonstrably perturbed) |
| D6 | `s16_feat` C-ROI | permuted node ROWS only; FC-profile COLUMNS kept their original order, so the control was not a consistent ROI relabelling | HIGH | the entire C-ROI result on A4 and A6 | INSIDE | symmetry, diagonal, feature/adjacency consistency, inverse reconstruction, band columns untouched | **FIXED (Gate 5)** — `apply_c_roi()` permutes adjacency `FC[p][:,p]`, node rows, the 90 profile columns, and ALFF rows, leaving the 3 band columns alone; B1-B8 pass |
| D6b | `s16_feat` `edgetri` early return | controls were silently inert for A7 | MEDIUM | any A7 control arm | INSIDE | a control must change A7's input | **FIXED (Gate 5)** — the triangle is now taken from the relabelled adjacency; B9 proves C-ROI changes A7's input |
| D7 | `s16_worker` alpha selection | on a tie `max` returned the SMALLEST alpha — the most learned-favouring choice | MEDIUM | every fused arm's `alpha_selected` and headline delta | INSIDE | a tied inner curve must resolve to the FC-favouring end | **FIXED (Gate 6)** — `max(inner, key=(auc, alpha))`; A1-A5 pass, A5 shows the old rule picked alpha=0.0 on a total tie |
| D8 | `s16_collect.py` | the arm x E pivot mixed F-LAB/F-SITE/F-LOSO into one mean | MEDIUM | the headline delta table | INSIDE | pivot must separate `fold_protocol` | **PARTLY ADDRESSED (Gate 4)** — the headline pivot was REMOVED from the collector, which now only writes the per-cell CSV carrying `fold_protocol`; no aggregated headline is produced at all until a reporting step is written. Still OPEN as a reporting requirement. |
| D9 | `_e2e_check.py:34` `abs(a-0.5) > 1e-9` | rejects an AUC of **exactly 0.5**, which is a legitimate and expected outcome for C-PERM (permuted labels). A correct control would be reported as a gate failure | MEDIUM | C-PERM gate verdict | INSIDE | a synthetic exactly-0.5 control row must PASS while `None`/`NaN` still FAIL | OPEN |
| D10 | `s16_worker.py:85` | `ema_sd` returned by `train_fold` and **never used**; EMA updated every step then discarded | **HIGH** (re-rated: it is a frozen-protocol violation, not a tidiness issue) | every fold row lacked `head_ema` and `ema_delta`, which S15 PROTOCOL.md:200 lists as a per-fold deliverable | INSIDE | evaluated state == documented state | **FIXED (Gate 2)** — EMA evaluated alongside raw per the frozen rule; `head_ema`, `ema_delta`, `evaluated_state` now recorded; B1-B3 pass |
| D11 | `s16_data.py:15` `GIT = "d52798c"` | hard-coded runtime SHA is **stale**; HEAD is `2ef2ce3`. Every provenance record written by every job would misattribute the code version | MEDIUM | provenance in all 18 existing rows and all future rows | INSIDE | SHA read from `git rev-parse HEAD` at runtime, or asserted equal to HEAD | OPEN |
| D12 | `C2_PROBE.md` | still presents the **superseded single-draw** pure-bias table (+0.0367 / +0.0331 / +0.0486) as the result; the 20-repeat refit and the FAILED calibration point (random encoder +0.0231 ± 0.0067, which sets a ~+0.023 estimator floor) appear only in `C2_PRECISION.md` | HIGH | the C2 conclusion as published on GitHub | INSIDE | `C2_PROBE.md` headline replaced by mean ± SE with the calibration offset applied and the estimator floor stated | OPEN |
| D14 | `C6_SHORT.md` "WHAT EACH FOLD REPORTS" | omits EMA entirely although S15 PROTOCOL.md:186,200 freezes "EMA and raw both evaluated ... both reported with the delta" | MEDIUM | the published description of the C6 recipe | INSIDE | recipe documentation lists every frozen per-fold deliverable | **FIXED (Gate 2)** — EMA clause added with its source citation |
| D15 | `test_gate3.py` (as first written) | every field-mutation case was rejected by the dirty-worktree guard BEFORE the field under test was compared, so 12 cases "passed" for the wrong reason — a masked test | **HIGH** (a test that cannot fail is worse than no test) | the Gate-3 evidence itself | INSIDE | each case must be rejected *for the reason under test* | **FIXED (Gate 3)** — mutations now run against a synthetic clean manifest and assert the rejection substring; dirty guard tested separately (C3) |
| D16 | `s16_collect.py` (pre-Gate-4) | the collector accepted any set of results that carried no failures, with no notion of an EXPECTED ledger; a wave missing 158 of 159 units would have been written out as CLEAN | **BLOCKER** | any results CSV produced before Gate 4 | INSIDE | 11 rejection classes each exit non-zero and write nothing | **FIXED (Gate 4)** — `s16_ledger.expected_ledger()` (159 units x 9 folds = 1431 cells, hash `8587b1ca36553408`) is now the authority; 12 intentional-failure tests all exit 3 |
| D17 | fusion wording in `C6_SHORT.md`, `S15_TERMINATED.md`, worker comments | the alpha=1 endpoint was called a **"guaranteed floor"**, implying the fused delta could not be negative. Nothing constrains the SELECTED alpha's outer-test performance | **HIGH** (unfalsifiable framing) | every statement of the C6 headline | INSIDE | wording states an ENDPOINT; delta explicitly unclamped | **FIXED (Gate 6)** — renamed "FC fallback endpoint"; `delta_is_unclamped=True` per cell |
| D18 | `s16_worker` baselines | `svm_tr_enc` was the only baseline and 0.7319 was treated as universal | **HIGH** | every delta in the wave | INSIDE | both baselines per fold, identical test subjects | **FIXED (Gate 6)** — `svm_tr_full` + `size_delta_paired`. Fold lab0: enc 0.7319, full 0.7490, paired **+0.0171** — NOT the historical +0.0246 |
| D19 | no subject-level output | aggregates were not reproducible without rerunning training | MEDIUM | all reporting | INSIDE | aggregates reproducible from saved predictions | **FIXED (Gate 6)** — `<unit>__<fold>.pred.json`, schema `s16-pred-1` |
| D20 | C2 estimator (`s16_c2_rescore.py`, `s16_c2_precision.py`) | the biased comparator drew from all 763 training subjects across every site while the honest comparator drew from ONE test fold — a diversity asymmetry, not memorisation. The random-encoder calibration read **+0.0231** where it must read 0 | **BLOCKER for interpretation** | every retrospective pure-bias number, and the claim that "only the two BNTs memorise" | INSIDE | random-encoder mean inside the predeclared band [-0.01,+0.01] | **ESTIMATOR REPLACED (Gate 7), NOT YET RUN** — `s16_c2_bounded.py` matches site x label exactly, samples without replacement, refuses rather than repairs. Feasibility CONFIRMED (6,671 checks, 0 infeasible). Earlier tables marked SUPERSEDED; the BNT-memorisation claim is WITHDRAWN pending calibration |
| D13 | `sb_bnt.sh`, `sb_wgin.sh`, `sb_ctrlu.sh` | contain **no** `--cpus-per-task`, `--mem`, `--time` or `--array`; those are supplied only on the `sbatch` command line, so re-running the script alone silently uses cluster defaults | LOW | reproducibility of the submission | INSIDE | resources declared inside the scripts | OPEN |

## NOT DEFECTS — verified correct at this baseline
- `--gres` absent from **all 9** submit scripts; `sinfo` shows only `gpu-l40s` and `gpu-h100`, so there is no CPU partition to target and CPU-only means "no `--gres`". Correct as built.
- Mass-failure abort present: POISON marker + `scancel` of the array above 5% failures, per-unit `TALLY.json`, stderr ERROR line.
- `fold_protocol` derivation uses `rstrip("0123456789")`, so `loso10`..`loso18` map to `loso` (the S15 defect is not reproduced here).
- A7 parity now asserted at **float32**, which is the precision S12A5 arm C actually consumed (`w_wave1.py:34`); f64 residual 2.98e-08 is pure quantisation.
- alpha=1.0 identity verified exact (0.00e+00) against `svm_tr_enc` on every fused E2E arm.

## JOBS 1873765 / 1873766 — no cancellation needed
Both are **finished**, so the question of cancelling them does not arise.
- `1873765` (A7 rerun array, tasks 10 and 11): **COMPLETED**, exit `0:0`, 6 s and 5 s.
- `1873766` (E2E check): **FAILED**, exit `1:0`, 5 s — correct behaviour; it exits non-zero because A7 failed.
Their recorded runtime SHA is `d52798c` (stale, see D11); actual HEAD is `2ef2ce3`.
The committed code **is** known-fatal for A7 because of D1, but nothing is running, so no
cancellation authorisation is required.

## BASELINE FACTS
HEAD `2ef2ce3` · branch `s16-verify` · `git status --short` empty · no uncommitted diff.
Grid: 21 configs x {plain,fused} x 3 seeds = 126 MAIN + 24 CTRL + 9 ABL = **159 units**;
9 folds per unit (lab 0-2, site 0-2, loso 0-2) = **1431 fold-runs**.
Arrays: A (BNT+EDGEMLP) 72 units, B (WGIN+ABL) 63, C (CTRL) 24.
Artifacts on disk: 18 fold JSONs, 18 UNIT.done, 18 TALLY.json, 18 STATUS.json, 16 npz
(422 MB), 16 ckpt (13 MB), 4 caches + 1 verify model (90 MB), 0 POISON, 0 results CSV.
**All 18 rows show `epochs_run` <= 4 and fold `lab0` only — they are FOUR-EPOCH E2E
artifacts, not C6 output.** 16 of them occupy filenames C6 will look for (D2+D3).
Cluster: CPU idle 151/512, GPU idle 4/48, partitions `gpu-l40s` and `gpu-h100` only.

---

# FINAL PRE-SUBMISSION PASS (from f2c237f) — NEW DEFECTS D22-D31

| ID | file / function | symptom | severity | affected | scope | required test | disposition |
|---|---|---|---|---|---|---|---|
| D22 | `s16_collect.py` | consumed `delta_vs_0p7565_SECONDARY`, a key the worker had already STOPPED producing at Gate 6 — table construction would have raised `KeyError` on the first fused cell | **BLOCKER** | any C6 CSV | INSIDE | realistic fused fixture builds a CSV; stale key absent from the schema | **FIXED** — collector consumes `delta_vs_svm_tr_enc` / `delta_vs_svm_tr_full`; H14-H17 pass |
| D23 | `s16_worker.py` resume | `exp["repr_dim"]` was read **out of the manifest being validated** — a circular check that could never fail | **BLOCKER** | every resume decision | INSIDE | wrong repr_dim must be rejected | **FIXED** — `P.expected_repr_dim(arch,kh,H,readout)` computes it independently (EDGEMLP 32, BNT K*H, WGIN 90*hidden); H27 passes |
| D24 | `_e2e_run.py` | imported `s16_worker` BEFORE applying the 4-epoch overrides, so training ran 4 epochs while `TRAIN_CONSTS` and provenance recorded **400** — the record was FALSE | **BLOCKER** | every E2E provenance record | INSIDE | E2E record must say 4 and production 400 | **FIXED** — immutable `s16_policy.ExecPolicy` drives training, config hash, epoch manifest, resume and record from ONE object; H5 proves both |
| D25 | `_e2e_run.py` targets | 26 labels over **25 unique unit IDs**: `ALFF-abl` and `ALFF-raw` selected the SAME unit, silently halving that coverage | HIGH | E2E coverage claims | INSIDE | labels, (branch,index), unit IDs and output paths all unique | **FIXED** — duplicate removed, `assert_targets_unique()` enforces all four; 29 unique targets; H3/H4/H33 pass |
| D26 | `s16_prov.validate_reuse` | validated only feature+checkpoint. A feature/checkpoint pair with **no result and no prediction** would have been counted as a valid reuse | **BLOCKER** | resume and collection | INSIDE | missing/corrupt prediction or result must reject | **FIXED** — `validate_bundle()` seals result + manifest + feature + checkpoint + prediction, each hash-checked; H20-H22 pass |
| D27 | `s16_prov` / `s16_collect` | `worktree_clean` was BOTH a guard ("must be True") AND a compared field ("must equal the live tree"). With a dirty tree **no bundle could ever validate**: `True` mismatches, `False` trips the guard | **BLOCKER** | all validation while uncommitted | INSIDE | a clean-tree artifact validates regardless of the tree's current state | **FIXED** — guard only; removed from `MATCH_KEYS`. The artifact's provenance is what matters, not the tree's later state |
| D28 | `s16_prov.build_manifest` vs `s16_collect` | both built `optimizer_recipe` independently and **diverged** (manifest omitted `batch`), rejecting all 1,431 cells | **BLOCKER** | every cell | INSIDE | fixture accepted | **FIXED** — both now call `policy.optimizer_manifest()`; single source of truth |
| D29 | `s16_c2_bounded.matched_draw` | crashed with `IndexError` on a malformed/empty pool instead of refusing | MEDIUM | C2 infeasibility handling | INSIDE | must return None, never raise | **FIXED** — casts to int and returns None; H35 passes |
| D30 | launchers | `sb_main.sh`, `sb_abl.sh`, `sb_ctrl.sh` overlapped the canonical three-array wave and could DUPLICATE units; resources lived only on the sbatch command line | HIGH | reproducibility, possible double-running | INSIDE | deprecated launchers refuse; canonical ones self-contained | **FIXED** — all three exit 2; canonical scripts embed partition/array/throttle/CPU/mem/time/requeue/signal/threads/namespace and assert the frozen unit counts; H11-H13 pass |
| D31 | validity gate wording | "each arm's own C-RAND twin" — **no such twin exists**. The grid has ONE C-RAND per architecture and NONE for EDGEMLP | HIGH | every validity verdict | INSIDE | mapping predeclared; A7 limitation stated | **FIXED** — `CRAND_MAPPING.md`: exact reference only for A4/A6; A1/A3/A5 flagged INPUT-MISMATCHED; **A7 cannot satisfy C-RAND and is descriptive only**; no control added silently; H39 passes |

## Corrected dispositions of earlier entries
- **D8** — reporting was described as "spec exists, unimplemented". `s16_report.py` NOW EXISTS and enforces protocol separation, the validity gate, C-PERM checks, per-architecture shift-vs-signed, unchanged negative deltas and exploratory C7 marking. **FIXED.**
- **D9** — E2E rejected AUC exactly 0.5. **FIXED**: only finite and within [0,1] is required.
- **D11** — `s16_data.GIT` hard-coded. **FIXED**: read at runtime; builder SHA recorded separately.
- **D12** — C2 supersession. **FIXED**: both reports carry a SUPERSEDED banner; superseded launchers refuse; bounded runner exists.
- **D13** — SLURM resources on the command line. **FIXED** (see D30).
- **Earlier claim corrected:** the Gate-4 ledger said the collector checked `UNIT.done`. It did not — it only counted files. It now checks per unit, with terminal STATUS and exact accounting. H19 proves it.

---

## Final Pre-Submission Correction Pass 2 (2026-08-25)

Baseline `bd0a0b45ee45e4512ce1219d848628c2d36273d0`, branch `s16-verify`, clean tree,
0 jobs in queue. Fourteen phases P1-P14. All seven spot-checkable defects named in the
commission were reproduced before any fix. No job was submitted, cancelled or requeued;
no C2, E2E or C6 run was executed; no production data, cache or prior evidence was
modified.

| ID | file / function | symptom | severity | affected | scope | required test | disposition |
|---|---|---|---|---|---|---|---|
| D32 | `s16_train.assert_groups_cover` | skipped frozen parameters (`if not p_.requires_grad: continue`) and then demanded every declared group be nonempty. In C-RAND, `inp` and `enc` are frozen, so the very next check raised `group 'inp' is empty` | **CRITICAL** | all 54 C-RAND cells (6 units x 9 folds); every validity verdict that depends on them | INSIDE | real `train_fold()` for BNT and WGIN C-RAND, not a synthetic fixture | **FIXED** — every parameter, frozen included, must map to exactly one group; only the TRAINABLE census may be empty, and only for the intentionally frozen groups; head must stay trainable and nonempty. Unknown/overlapping/missing-prefix detection strengthened, not weakened. `test_pass2_p1p2.py` 26/26 |
| D33 | `s16_models` BNTR/WGINR/EdgeMLP | `freeze_encoder` set `requires_grad=False` but left the encoder in TRAIN mode, so encoder dropout kept resampling. A "fixed random encoder" was not fixed and its representation was not reproducible | **HIGH** | all C-RAND cells; the untrained watermark those cells define | INSIDE | eval mode under `.train()`; bitwise-identical repeated representations; encoder weights+buffers unchanged; head changed | **FIXED** — `train()` override forces frozen encoder submodules to eval; verified bitwise (max delta 0.000e+00) for both architectures, and unfrozen models still train their encoders |
| D34 | `s16_worker` resume path | rebuilt `epoch_policy`/`optimizer_recipe` inline and the inline recipe OMITTED `batch`, so a worker expectation could never equal a `build_manifest()` record: every resumable cell would silently recompute. The collector held a THIRD copy of the same dict, and `_cfg_for` duplicated the optimizer literals in a second file | **HIGH** | resume correctness across all 1,431 cells; wasted recomputation | INSIDE | round-trip reuse + 4 field mutations + informational-field non-rejection | **FIXED** — `P.contract_fields()` builds the 28 MATCH_KEYS in ONE place; worker, manifest and collector all call it; `ExecPolicy.train_consts()` and `P.model_cfg()` are the single owners. `namespace` is now explicit in the result record and REQUIRED by the collector (no `.get(...,ns)` fallback). `test_pass2_p3.py` 17/17 |
| D35 | `s16_collect.audit` | `DAT.load("signed")` was called ONCE and every unit validated against it, so `h_fc` and `cache_file` were structurally wrong for every `abs`/`pos_zero`/`shift` cell | **CRITICAL** | **810 of 1,431 cells (56.6%)** — every cell of the three non-signed E levels (3 of 4 levels) would have been rejected as provenance failures | INSIDE | mixed-E fixture; all 810 non-signed cells validate | **FIXED** — per-E metadata cache. A real 159-unit x 9-fold fixture with distinct per-E hashes collects all 1,431 cells; replaying the old single-signed binding rejects **exactly 810**, every one an `h_fc`/`cache_file` mismatch. `test_pass2_p4.py` 7/7 |
| D36 | `s16_c2_bounded_run` | wrote `C2_BOUNDED.json` and `C2_BOUNDED_LEDGER.json` into `audit/s16/out/`, which is NOT gitignored, so a C2 run dirtied the worktree — and a dirty tree is what the provenance guard refuses | MEDIUM | C2 runs; every bundle produced afterwards | INSIDE | clean tree before and after | **FIXED** — runtime products go to the ignored `audit/s16/runs/c2/` via TEMP->validate->rename; `git check-ignore` confirms both paths ignored and `git status` stays clean |
| D37 | `s16_report.refusals` | `_,cells,units,tags = L.expected_ledger()` unpacked 4 values from a function returning 3 | **CRITICAL** | the report crashed with `ValueError` on its first statement — it could never have emitted a headline on any input | INSIDE | a complete table must produce a nonempty report | **FIXED** — correct 3-value unpack; the report runs end to end and emits 32,750 characters over all three estimands. `test_pass2_p6789.py` 23/23 |
| D38 | `s16_report.validity` | grouped by `(arm,E,mode)` and compared against a C-RAND reference POOLED over lab+site+loso | **HIGH** | every validity verdict; LOSO runs ~0.044 below the ordinary protocols, so a pooled reference imported that shift into the decision | INSIDE | per-protocol grouping; the protocol shift must cancel | **FIXED** — decision level is `(arm,E,mode,fold_protocol)`; statistic is per-seed fold-mean then mean across seeds; the C-RAND reference is same-arch AND same-protocol. The pass rule is now written out in the docstring |
| D39 | `s16_report.shift_vs_signed` | averaged the signed cells and the shift cells SEPARATELY and subtracted the two means, mixing the identity with whatever composition differed between the two sets | MEDIUM | the shift ~ signed claim | INSIDE | paired on `(arch,arm,mode,seed,fold_protocol,fold)`; A3 excluded | **FIXED** — inner-joined pairs, per-pair differences, unpaired cells reported explicitly. A3 excluded with the reason stated: its node features ARE the FC rows, so E rewrites the inputs rather than only the graph weights |
| D40 | `s16_report.main` | C-PERM and the shift identity were PRINTED as diagnostics and never enforced; a leaking control could not stop the report | **HIGH** | the integrity of every headline | INSIDE | a broken control must exit nonzero | **FIXED** — both are hard gates exiting 4. Bands **pre-registered 2026-08-25 before any C6 result exists**: C-PERM protocol-mean in [0.45, 0.55]; BNT paired shift-signed within +/-0.01. A C-PERM of exactly 0.500 PASSES (D9 semantics preserved) |
| D41 | `_e2e_check.py`, `sb_e2e_chk.sh` | read only the FIRST `fold_*.json` per unit and checked a handful of AUC fields; a unit could carry the wrong fold, an extra fold, a broken sealed bundle, a PROD policy hash or a missing prediction and still report PASS. The submit script never set `S16_NS` | **HIGH** | the E2E gate that authorises the production wave | INSIDE | full contract for all 29 targets | **FIXED** — exact fold set, explicit namespace, policy identity, grid identity, epoch budget, repr width, the sealed 5-file bundle, subject-resolved predictions and the fused contract are all checked per target; `sb_e2e_chk.sh` exports `S16_NS=e2e` and four thread caps. NOT executed — running E2E is held pending authorization |
| D42 | `s16_c2_bounded_run.main` | validation and fitting shared one loop, so sources 1-6 were fitted before source 7 was found invalid | MEDIUM | C2 runtime; a partial source set invites choosing the estimate after seeing it | INSIDE | zero probe calls when source seven is invalid | **FIXED** — Phase 1 validates all seven, Phase 2 fits only if all pass. With source seven invalid, `probe_pipe` is called **0 times**, exit 6, the six valid sources recorded as `NOT_RUN`. `test_pass2_p11.py` 11/11 |
| D43 | `s16_c2_bounded_run.validate_source` | accepted `ids_ref` and never used it; equal label vectors do not prove equal subject ORDER | MEDIUM | subject alignment across seven retrospective sources | INSIDE | a renamed/permuted id must be caught with identical labels | **FIXED** — subject order compared when a source records ids; an unreadable id array is a problem, not a crash. The six real sources carry no id array, and that is now stated explicitly rather than assumed away |
| D44 | `s16_c2_bounded_run.validate_source` | checked only that tr/te were disjoint and totalled 954. A duplicate inside tr offsets a missing subject, so one subject is scored twice and another never | MEDIUM | every C2 fold | INSIDE | a crafted disjoint 954-total non-partition must be caught | **FIXED** — exact partition of 0..953 plus per-side duplicate detection; the crafted split is rejected and named |
| D45 | `s16_worker` stop path, `sb_*.sh` | on SIGUSR1 the worker wrote status `requeued` and exited 0. SLURM does not requeue a clean exit, so the unit stopped for good while the record claimed it would resume. Worse, `--signal=B:USR1@300` delivers to the BATCH SHELL, not the python child, so the handler could never fire: the whole graceful-stop path was unreachable | **HIGH** | truthfulness of unit status; silent loss of units on preemption | INSIDE | requeue must be explicit and its real outcome recorded | **FIXED** — `requeue_self()` issues `scontrol requeue $SLURM_JOB_ID`, records the job id, return code and stderr, and reports `stopped_not_requeued` whenever the requeue did not happen; exit 0 only on a real requeue, else 3. The three canonical launchers trap USR1 and forward it to the child, propagating its exit status. Verified against a mocked `scontrol` and a live signal-forwarding harness. **No cluster job was submitted, cancelled or requeued.** `test_pass2_p13.py` 19/19 |
| D46 | `s16_worker` result record | the P3 fix initially passed `policy_hash` explicitly while `**info` already supplied it — `dict() got multiple values for keyword argument 'policy_hash'`, the exact failure class that killed all 288 S15 units | **HIGH** | would have killed every PROD cell | INSIDE | caught by `test_gate3.py` before any submission | **FIXED** — only `namespace` is added explicitly; `policy_hash`/`policy_name` come from `info`, emitted by `train_fold` from the same policy object |

### Environment finding (not a code defect)

The `/users` quota is **exceeded**: 270G against a 250G soft quota and a 300G hard
limit, with 4 days of grace remaining, and 1,427k files against a 1,500k soft limit.
Writes were failing silently mid-fixture, which is what produced a spurious
`FileNotFoundError` in `test_final.py` before the scratch namespace was cleared. The
space is dominated by unrelated projects, not by this audit. A PROD wave writes
~1,431 sealed bundles; at the ~16 MB per cell observed in the quarantine ledger that
is roughly **23 GB**, against ~30 GB of headroom before the hard limit. This must be
resolved before submission.

### Test-side corrections made in this pass

- `test_gate2.py` updated to the two-census `assert_groups_cover` API (D32).
- `test_final.py` fixture rebuilt with PER-E metadata. It had been constructing every
  unit's manifest from the signed cache, encoding the very bug D35 fixes.
- `_e2e_check.py` keeps the variable name `a` so gate 8's literal assertion for
  "AUC exactly 0.5 is valid" (D9) continues to hold unchanged.

---

## Pass 3 — final bounded correction (2026-08-26)

Branch `s16-pass3`, parent `02cb94c` (`origin/main`, which merged the Pass-2 commit
`dd43503` via PR #2). Every defect below was REPRODUCED by a failing test before any
repair; `repro_pass3_historical.py` (renamed from `test_pass3_repro.py` in Pass 4,
defect D56) is that evidence and now exits 1 with all six lines
reading NOT REPRODUCED, which is the inversion proof the repairs landed.

| ID | file / function | symptom | severity | affected | scope | required test | disposition |
|---|---|---|---|---|---|---|---|
| D47 | `s16_c2_bounded_run.validate_source` | every fold was validated in ISOLATION, so five files each holding the SAME `te` passed — each is a legal partition on its own. Five copies of one fold were accepted as five valid folds: the same 154 subjects "held out" five times while **800 of 954 subjects are never scored once**. Nothing compared fold membership ACROSS the seven sources either, so two sources could be paired over different subjects | **CRITICAL** | every C2 estimate; the paired difference would be computed over the wrong subjects | INSIDE | five identical folds, duplicate membership, missing subjects, overlapping folds, wrong train complement, cross-source mismatch, zero fitting on any invalid source | **FIXED** — five distinct test signatures, pairwise-disjoint test sets, union exactly `0..953`, each `tr` exactly the complement of its `te`, and one canonical membership shared by all seven sources. `test_pass3.py` |
| D48 | `_e2e_check.py` | approved a target on SEALED-BUNDLE evidence alone. The file contained **zero** references to `POISON`, `TALLY.json`, `STATUS.json` or `UNIT.done`, so a unit that was poisoned, recorded failures, or never reached a terminal state was still reported PASS. A sealed bundle proves one FOLD is correct; it says nothing about whether the UNIT finished | **HIGH** | the E2E gate that authorises the production wave | INSIDE | remove or corrupt each completion artifact and prove rejection | **FIXED** — `P.validate_unit_completion()` is ONE shared contract called by both the collector and the E2E checker, so the two definitions cannot diverge. It enforces global and unit `POISON` absence, readable `TALLY.json`, `failed == 0`, `remaining == 0`, the accounting identity `validated_reused + newly_succeeded == expected_folds`, terminal `STATUS.json`, and `UNIT.done`. E2E additionally requires the frozen evaluated state and finite `head_ema` |
| D49 | `s16_report.shift_gate` | treated affine representational equivalence as if it required two INDEPENDENTLY initialised and optimised models to reach nearly equal AUC, and enforced `\|shift − signed\| <= 0.01` as a hard gate exiting 4 | **HIGH** | headline validity — the gate could have failed a CORRECT run | INSIDE | measure the seed-to-seed noise floor; replace with a deterministic transport test that fails under an incorrect transport | **FIXED** — measured noise on identical data at a fixed E level with only the seed changed is **0.0820 AUC, eight times the 0.01 tolerance**, so the gate could not distinguish a broken identity from ordinary optimisation variance. The AUC gate is WITHDRAWN. The claim is now tested deterministically: transport `inp` by `W' = 2W`, `b' = b − W·1`, copy all other parameters, run in eval mode. Correct transport residual **1.57e-05**; incorrect transport (bias uncorrected) **7.98e+01** — a 5.1-million-fold contrast, so the test has real power. Trained `shift − signed` AUC differences remain in the report as DESCRIPTIVE diagnostics; pair completeness stays mandatory |
| D50 | `s16_report.cperm_gate` | the failure message asserted "permuted labels are being predicted, which means the pipeline leaks" — stating leakage as a PROVEN cause when an out-of-band permutation result only establishes that investigation is required | MEDIUM | the scientific claim attached to a hold | INSIDE | the message must not assert proven leakage | **FIXED** — the `[0.45, 0.55]` band remains an operational hold-and-investigate gate (legitimate because no C6 result exists). The message now reads: outside the predeclared operational band; STOP headline generation and investigate permutation variance, class balance, aggregation, optimisation and possible leakage; this result alone does NOT prove leakage |
| D51 | `DEFECTS_FOUND.md`, `CORRECTION_PASS2_2026-08-25.md`, `test_pass2_p4.py` | stale arithmetic: the 810 non-signed cells were described as a larger share of the grid than the ledger supports — they are 56.6% of 1,431, whereas a 66.7% share would be 954 cells — and D32 said "all 24 C-RAND control cells", conflating the 24 CONTROL UNITS with C-RAND's own cell count | LOW | the accuracy of the record | INSIDE | every count recomputed from the frozen ledger | **FIXED** — derived mechanically: C-RAND **6 units × 9 folds = 54 cells**; all controls **24 units × 9 = 216 cells**; non-signed is **3 of 4 E levels** = 810 cells = **56.6%** of 1,431 |
| D52 | `s16_c2_bounded_run.main` (introduced by the D47 fix) | the cross-source membership problem was appended to the global `problems` list, but the halt condition inspected only PER-SOURCE problems, so a cross-source mismatch fell through to fitting and exited 5 at calibration instead of 6 before any fit | **HIGH** | would have defeated the D47 fix | INSIDE | a cross-source mismatch must exit 6 with zero probe calls | **FIXED** — the halt now covers both per-source and cross-source problems. Found by `test_pass3.py` while testing the D47 fix itself |

---

## Pass 4 — surgical false-pass correction (2026-08-26)

Branch `s16-pass4`, parent `b9a6025`. Every defect was REPRODUCED before repair by
`repro_pass4.py` (14/14, exit 0); that file now exits 1 with every line NOT
REPRODUCED. It is deliberately named `repro_*`, not `test_*` — that naming is D56.

| ID | file / function | symptom | severity | affected | scope | required test | disposition |
|---|---|---|---|---|---|---|---|
| D53 | `s16_prov.validate_unit_completion` | `remaining` was accepted when it was `0` **or `None`**, and `dict.get` returns `None` for an ABSENT key — so a TALLY that never recorded how much work was left passed as complete. `False` also passed every numeric test because in Python `False == 0`, and string counts passed untouched | **HIGH** | any unit whose tally is truncated or malformed could be certified complete; the E2E gate and the collector both trusted it | INSIDE | absent, null, boolean, string, negative and nonzero `remaining`, plus the same for `expected`, `validated_reused`, `newly_successful`/`newly_succeeded` and `failed` | **FIXED** — every count must be PRESENT, a real non-boolean `int`, and nonnegative; `remaining` must be exactly 0; `failed` must be 0; counts may not exceed `expected_folds`; the identity `validated_reused + newly_succeeded == expected_folds` is preserved, and its message still contains the pre-existing "reused N + new M != expected K" wording so `test_final`'s H49 holds unchanged. 19 negative cases in `test_pass4.py` |
| D54 | `s16_report.shift_gate` | took `unpaired` as a parameter and never referenced it. A table built from a partly-unpaired set reported a clean paired difference computed over whichever cells happened to match, and nothing stopped the headline | **HIGH** | the shift-vs-signed diagnostic and, through it, headline release | INSIDE | nonempty `unpaired` must stop; zero pairs must stop; fully paired must pass at ANY AUC magnitude | **FIXED** — a nonempty `unpaired` list and any zero-pair group are hard failures exiting 4. The withdrawn ±0.01 AUC gate is **not** restored: a fully paired 0.30 AUC difference still exits 0 and is labelled descriptive |
| D55 | `s16_collect.audit`, `s16_report.refusals` | the collector validated the sealed bundle and three scalar fields; `eval_point` existed only as a CSV column name inside `build_rows`, and `evaluated_state` was only ever copied into the CSV, never compared to anything. The report checked only that `(unit, fold)` appeared SOMEWHERE in the CSV. A cell missing three of its four evaluation rows, or carrying a NaN AUC, or a plain cell smuggling a fusion block, passed both | **CRITICAL** | any headline could rest on cells that were never fully evaluated | INSIDE | hash-consistent bundles with each metric removed, each AUC set NaN/Inf/out-of-range, movement and clip NaN, and CSV rows removed, duplicated or invented | **FIXED** — `P.validate_eval_contract()` is one shared contract: all four metric dictionaries present with finite AUC in `[0,1]`; fused cells carry exactly one finite `fused_auc` in `[0,1]`; plain cells carry none; `movement_max` and `clip_rate` finite; `evaluated_state` matches the frozen raw/EMA protocol. The report independently verifies the EXACT per-cell evaluation-point set with new rejection classes `eval_point_set_wrong` and `duplicate_eval_point`. 34 adversarial cases |
| D56 | `test_pass3_repro.py` | an intentionally-failing file carried the `test_` prefix, so standard discovery reported a permanent failure and trained reviewers to ignore red | MEDIUM | the credibility of the whole suite | INSIDE | every `test_*.py` must exit 0 on correct code | **FIXED** — renamed to `repro_pass3_historical.py` (git mv, history preserved) with a docstring stating that exit 1 means the fixes are in place. `repro_pass4.py` follows the same convention. A SECOND instance of the same defect was found during Pass-4 verification: `test_gate4.py` was a retired stub that deliberately exited 2 "so it can never be reported as a silent pass" — also renamed, to `retired_gate4_superseded.py`. All 15 remaining `test_*.py` files exit 0 |
| D57 | `s16_report.shift_vs_signed` | **found in Pass 4 while fixing D54.** The pairing key omitted `alff_mode`, so the ABLATION units (A1 with `alff-raw` / `alff-joint`) collided with the main A1 units on `(arch, arm, mode, seed, fold)`. The inner join went many-to-one and produced **351 "pairs" out of only 270 shift cells** — signed ablation rows were silently duplicated against main shift rows, and the unpaired count went negative | **HIGH** | every shift-vs-signed number reported since the Pass-3 D39 fix | INSIDE | the pair count must never exceed either side | **FIXED** — `alff_mode` added to `PAIR_KEY` and `UNIT_KEY`. Separately, the pairing DOMAIN is now the set of unit-keys present at BOTH E levels: only 30 of the 69 signed unit-keys have a shift twin in the frozen grid (controls and ablations exist at `signed` only, by design), so counting those as unpaired would have made the D54 gate fire on every correct run — the same over-strictness that made the ±0.01 AUC gate unusable |

### E2E wall-time evidence (requested; not changed blindly)

| | |
|---|---|
| source | `sacct` array `1873735` (`s16e2a`), 2026-08-24, **22 COMPLETED** tasks |
| maximum observed elapsed | **00:17:51** (1,071 s) |
| current limit `sb_e2e_arr.sh` | `--time=02:00:00` (7,200 s) |
| **safety margin** | **6.7×** — the required 2× is satisfied |
| partition maximum | `gpu-l40s` and `gpu-h100` both `7-00:00:00` |

The wall time is **left unchanged at 2 hours**. Two caveats stated rather than
assumed: that array recorded 22 completed tasks against the current 29 targets, and
the current worker does slightly more per fold than the 2026-08-24 build (EMA
evaluation, subject-level predictions, five-file bundle sealing) — additional I/O
and one extra forward pass, not additional training. Even a 3× slowdown stays inside
the limit. This is **not** an operational blocker.

Runtime evidence was taken from scheduler accounting only. The five E2E records now
on disk are from the array cancelled at 20:13 today and were **not** inspected.

---

## D58 — Gate 3 depended on ambient Git state (2026-08-26)

Branch `s16-d58`, parent `fd3bf215d2419871e6af591896b6af9e89ab4c68`.

| ID | file / function | symptom | severity | affected | scope | required test | disposition |
|---|---|---|---|---|---|---|---|
| D58 | `test_gate3.py::D3_dirty_tree_forces_recompute` | the assertion read `not P.worktree_clean()[0]` — it required the LIVE repository to be dirty. It therefore passed throughout Passes 2, 3 and 4 only because correction work was uncommitted at the time, and FAILED the moment the suite was first run against the clean committed tree that a production run demands. The guard under test is a property of the MANIFEST, not of the ambient checkout, so binding it to the working directory made a green suite conditional on the developer's editor state | **HIGH** | the credibility of Gate 3, and of every earlier "all tests pass" claim made from a dirty tree | INSIDE | D3 must pass identically whether the live repository is clean or dirty | **FIXED** — D3 now builds a hash-consistent manifest, stamps `worktree_clean=False` with synthetic `worktree_dirt`, keeps every contracted field matching, calls the REAL `P.validate_reuse()`, and requires both rejection and a reason naming the dirty-manifest guard (`"manifest was produced from a DIRTY worktree"`). Verified passing with the live tree dirty AND clean. No production code changed; `s16_prov.py` untouched; no validation rule weakened; the test is not inverted — it still asserts that a dirty-stamped artifact forces recomputation |

This is the mirror image of D56. D56 was a file that could never pass on correct code;
D58 was a check that could only pass on INCORRECT (uncommitted) repository state. Both
made a red or green result mean something other than what a reviewer would assume.
